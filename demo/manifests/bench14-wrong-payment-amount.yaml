# Benchmark regression 14: the authorization is sent for an amount of 0.
#
# An outbound-surface test. The app routes through the proxy (env_var points
# its own PAYMENT_URL at it), and only the request body on the wire changes:
# payment_calls still records the real amount on both sides, so the recorded
# call is the only place the provider is being told the wrong number.
app:
  name: shop-api
  start: uvicorn app.main:app --host 0.0.0.0 --port 8000
  port: 8000
  healthcheck: /health
  dockerfile: Dockerfile

database:
  type: postgres
  seed: seed.sql
  observe_tables:
    - payment_calls

outbound:
  services:
    - name: payment-provider
      base_url: https://api.payments.example.com
      env_var: PAYMENT_URL      # the variable the app already reads
      mock_responses:
        POST /v1/authorize:
          status: 200
          body:
            status: approved
            auth_id: mock_001

compare:
  base_ref: main
  target_ref: bug/wrong-payment-amount
  repo: ../.demo-repo  # relative to this manifest; built by demo/build_demo_repo.py

workflows:
  - name: checkout-a-valid-cart
    steps:
      - method: POST
        path: /api/carts
        body: {"items": [{"sku": "SHOE-42", "qty": 1}]}
        capture: {"cart_id": "$.cart_id"}
      - method: POST
        path: /api/checkout
        body: {"cart_id": "{cart_id}", "address": {"street": "123 Main", "city": "SF", "zip": "94102"}}

normalize:
  ignore_fields:
    - "*.created_at"
    - "*.updated_at"
    - "headers.date"      # response clock, not behaviour
  uuid_fields:
    - "*.cart_id"
    - "*.order_id"
    - "*.id"
  numeric_tolerance: 0.001
  ignore_row_order:
    - payment_calls
