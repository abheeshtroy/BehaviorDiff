# BehaviorDiff

A change-aware differential testing engine that runs two versions of an application under identical conditions and compares their observable behavior — HTTP responses, database state, outbound calls, and latency.

**Work in Progress**

## Built so far

- Manifest parser with strict validation (38 tests)
- Docker orchestrator with lifecycle management (10 tests)
